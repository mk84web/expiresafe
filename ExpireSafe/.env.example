# Flask
SECRET_KEY=change-me-to-a-long-random-string
FLASK_ENV=development

# Stripe (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ESSENTIAL=price_...
STRIPE_PRICE_ENFORCED=price_...
STRIPE_PRICE_GOVERNED=price_...

# Email (SendGrid example)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.xxxx
SMTP_FROM=noreply@yourdomain.com

# App
APP_BASE_URL=https://yourdomain.com
SUPERADMIN_EMAIL=your@email.com
SIGNUPS_ENABLED=true

# Optional: persistent storage for cloud (Render, Railway, etc.)
# PERSISTENT_STORAGE_PATH=/var/data

# Optional: auto-delete documents older than N days (0 = disabled)
# RETENTION_DAYS=0
